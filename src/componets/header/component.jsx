import style from './styles.module.scss';

export const Header = () => {
    return (
        <div className={style.root}>
            <div className={style.header}>header</div>
        </div>
    );
};
